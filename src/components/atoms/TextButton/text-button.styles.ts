import {TouchableOpacity} from 'react-native';
import styled from 'styled-components/native';
import {Text as CText} from '../Text/text';

export const Container = styled(TouchableOpacity)``;

export const Text = styled(CText)``;
